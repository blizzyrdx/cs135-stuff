#include <iostream> //g++ main.cpp (g++ is the name of the compiler and main.cpp is the name of the file to be compiled)
using namespace std; // to execute use ./a.out

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}